#if !defined(HEXADECIMAL_H)
#define HEXADECIMAL_H

#include <iostream>
#include <cmath>
#include <map>

namespace hexadecimal {
    int convert(std::string);
}  // namespace hexadecimal

#endif // HEXADECIMAL_H